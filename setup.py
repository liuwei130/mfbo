from setuptools import setup, find_packages

setup(
    name='mf-mes',
    version='0.1',
    description='A package for the experiments for Bayesian optimization in several settings',
    packages=find_packages()
)